# RaspBerry Pi temperature sensor
RaspBerry Pi temperature sensor for Windesheim nerdygadgets kbs

# Use
change host and port in py script if needed.
Run tempsensor_client.py with provided venv or other installation with requirements.
Run tempsensor_server.py on server.

## Requirements
* Python3+
* mysql + mysql-connector module
* sense_hat module
